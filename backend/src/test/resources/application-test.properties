# src/test/resources/application-test.properties

# Baza danych (skonfigurowana pod kontener na GitHub Actions)
spring.datasource.url=jdbc:postgresql://localhost:5432/vod_db
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# Bezpieczne ustawienia bazy dla testów
spring.jpa.hibernate.ddl-auto=none
spring.flyway.enabled=true

# Mocki do sekretów (rozwiązanie błędu PlaceholderResolutionException)
security.jwt.secret-key=super-tajny-klucz-jwt-do-testow-o-odpowiedniej-dlugosci
jwt.secret=super-tajny-klucz-jwt-do-testow-o-odpowiedniej-dlugosci
tmdb.api.key=mock-key-for-tests
openai.api.key=mock-key-for-tests